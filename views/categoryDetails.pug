extends layout 

block content 
  h1= title
  ul 
    for item in items
      li
        a(href= item.url)= item.name
    else 
      a(href=`/categories/delete/${category._id}`) No Associated Items. Delete?